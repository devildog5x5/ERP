# Windows installers

Built by `..\build_installers.ps1` on a Windows machine with Inno Setup 6.

| File | Role |
|------|------|
| `CoalesceSetup.exe` | Chooser — Both / Server / Client |
| `CoalesceClientSetup.exe` | Client only |
| `CoalesceServerSetup.exe` | Server only |

Published copies live on [GitHub Releases](https://github.com/devildog5x5/ERP/releases). Checked-in EXEs may lag installer source until the next Windows rebuild.
