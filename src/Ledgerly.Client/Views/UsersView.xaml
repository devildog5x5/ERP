<UserControl x:Class="Ledgerly.Client.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:svc="clr-namespace:Ledgerly.Client.Services">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="160"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="Add user" Style="{StaticResource PrimaryButton}" Click="Add_Click" Margin="0,0,8,0"/>
            <Button Content="Reset password" Style="{StaticResource SecondaryButton}" Click="ResetPassword_Click" Margin="0,0,8,0"/>
            <Button Content="Delete user" Style="{StaticResource DangerButton}" Click="Delete_Click"/>
            <TextBlock Text="Administrators manage accounts, reset passwords, and assign roles."
                       Foreground="#667888" VerticalAlignment="Center" Margin="16,0,0,0" TextWrapping="Wrap"/>
        </StackPanel>

        <DataGrid Grid.Row="1" x:Name="Grid" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal" BorderThickness="0" CanUserAddRows="False" RowHeight="36">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Username" Binding="{Binding UserName}" Width="140"/>
                <DataGridTextColumn Header="Display name" Binding="{Binding DisplayName}" Width="*"/>
                <DataGridTextColumn Header="Role" Binding="{Binding RoleName}" Width="140"/>
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="2" Text="Roles &amp; element access" FontWeight="SemiBold" Margin="0,16,0,8"/>
        <DataGrid Grid.Row="3" x:Name="RolesGrid" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal" BorderThickness="0" CanUserAddRows="False" RowHeight="36">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Role" Binding="{Binding Name}" Width="140"/>
                <DataGridTextColumn Header="Element access (permissions)" Binding="{Binding Permissions}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
