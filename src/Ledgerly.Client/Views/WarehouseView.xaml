<UserControl x:Class="Ledgerly.Client.Views.WarehouseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:svc="clr-namespace:Ledgerly.Client.Services">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <TextBlock Text="Touch-friendly warehouse tools" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,12"/>
            <WrapPanel Margin="0,0,0,16">
                <Button Content="Transfer stock" Style="{StaticResource PrimaryButton}" Margin="0,0,10,10" Padding="20,16" Click="Transfer_Click" svc:Access.Permission="warehouse"/>
                <Button Content="Cycle count" Style="{StaticResource SecondaryButton}" Margin="0,0,10,10" Padding="20,16" Click="Count_Click" svc:Access.Permission="warehouse"/>
                <Button Content="Add location" Style="{StaticResource SecondaryButton}" Margin="0,0,10,10" Padding="20,16" Click="AddLoc_Click" svc:Access.Permission="locations"/>
                <Button Content="Build kit (BOM)" Style="{StaticResource SecondaryButton}" Margin="0,0,10,10" Padding="20,16" Click="BuildBom_Click" svc:Access.Permission="warehouse"/>
            </WrapPanel>
            <TextBlock Text="Stock by location" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <DataGrid x:Name="LocGrid" AutoGenerateColumns="False" IsReadOnly="True" Height="220"
                      HeadersVisibility="Column" GridLinesVisibility="Horizontal" BorderThickness="0" CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Location" Binding="{Binding LocationCode}" Width="100"/>
                    <DataGridTextColumn Header="SKU" Binding="{Binding ProductSku}" Width="120"/>
                    <DataGridTextColumn Header="Product" Binding="{Binding ProductName}" Width="*"/>
                    <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="80"/>
                </DataGrid.Columns>
            </DataGrid>
            <TextBlock Text="Locations" FontWeight="SemiBold" Margin="0,16,0,8"/>
            <DataGrid x:Name="LocationsGrid" AutoGenerateColumns="False" IsReadOnly="True" Height="140"
                      HeadersVisibility="Column" BorderThickness="0" CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Code" Binding="{Binding Code}" Width="100"/>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Bin" Binding="{Binding Bin}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
